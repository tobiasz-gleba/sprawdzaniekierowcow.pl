# Database Configuration
# MySQL connection string format: mysql://username:password@host:port/database
DATABASE_URL=mysql://root:mysecretpassword@localhost:3306/local

# Email Configuration (SMTP)
# The email address to send emails from
EMAIL_USER=your-email@example.com

# The password or app-specific password for the email account
EMAIL_PASS=your-email-password

# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# SMTP server port (usually 587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT=587

# Application Configuration
# The public base URL of your application (used in email links)
# In development: http://localhost:5173
# In production: https://your-domain.com
PUBLIC_BASE_URL=http://localhost:5173

# Alternative to PUBLIC_BASE_URL (SvelteKit automatically sets this)
# ORIGIN=http://localhost:5173

# Node Environment (development, production, test)
NODE_ENV=development

# Server Configuration
# Port the application will listen on
PORT=3000

# Host the application will bind to
HOST=0.0.0.0

# ========================================
# GMAIL SPECIFIC NOTES:
# ========================================
# If using Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" at: https://myaccount.google.com/apppasswords
# 3. Use the generated app password as EMAIL_PASS
#
# Example Gmail configuration:
# EMAIL_USER=yourgmail@gmail.com
# EMAIL_PASS=abcd efgh ijkl mnop  (16-character app password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# ========================================
# OUTLOOK/OFFICE 365 NOTES:
# ========================================
# Example Outlook configuration:
# EMAIL_USER=youremail@outlook.com
# EMAIL_PASS=your-outlook-password
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# ========================================
# OTHER SMTP PROVIDERS:
# ========================================
# SendGrid: smtp.sendgrid.net (port 587)
# Mailgun: smtp.mailgun.org (port 587)
# Amazon SES: email-smtp.region.amazonaws.com (port 587)
